@inherits LoginModuleBase;
@using server.Components;

@*
	TODO:
	[DONE] List users
	[DONE] Click user to login
	[DONE] Parents will show password input
	[DONE] Do actual password check
	[TODO] fix name on card
	[TODO] fix nicer notification when loading in users (we have it on create task page)
*@
<div class="card-container">
	@if (Users == null)
	{
		<p>Getting users...</p>
	}
	else
	{
		@foreach (var user in Users)
		{
			<LoginCard User="@user" OnSuccess="async (a) => await Login(a)" />
		}
	}
</div>

